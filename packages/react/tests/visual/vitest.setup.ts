// Visual tests setup file
// - No @testing-library/jest-dom needed: Vitest browser mode provides its own DOM matchers
// - No cleanup() needed: vitest-browser-react handles cleanup automatically
// - document.fonts.ready is awaited per test file in beforeAll
